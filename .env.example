# Porta HTTP do backend (opcional, default 10000 no código)
PORT=10000

# TTL em dias das mensagens no Mongo (0 = sem TTL / sem expiração)
MSG_TTL_DAYS=0

# Id do cliente do WhatsApp Web JS (para diferenciar sessões, se quiser)
WWEBJS_CLIENT_ID=whatsapp-bot
